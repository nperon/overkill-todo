import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DialogOpeningButtonComponent } from './dialog-opening-button.component';

describe('DialogOpeningButtonComponent', () => {
  /* let component: DialogOpeningButtonComponent;
  let fixture: ComponentFixture<DialogOpeningButtonComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DialogOpeningButtonComponent ],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DialogOpeningButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
